// =============================================================================
// SchemaLoadState — flags enum (bitmask), keep numeric
// =============================================================================

export enum SchemaLoadState {
  None = 0,
  System = 1,
  Service = 2,
  Remote = 4,
}
