// =============================================================================
// Record — registers recorded values for enum-like properties
// =============================================================================

import { Property } from '../property';

export class Record extends Property<Function> {}
